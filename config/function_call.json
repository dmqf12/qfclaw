[
  {
    "type": "function",
    "function": {
      "name": "read",
      "description": "读取本地文件系统中的文件内容",
      "parameters": {
        "type": "object",
        "properties": {
          "file": { "type": "string", "description": "文件的绝对或相对路径" }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "write",
      "description": "向文件写入内容",
      "parameters": {
        "type": "object",
        "properties": {
          "file": { "type": "string", "description": "文件名，可包含绝对或相对路径" },
          "text": { "type": "string", "description": "要写入文件的内容" }
        },
        "required": ["file", "text"]
      }
    }
  },
  {
  "type": "function",
    "function": {
      "name": "exec",
      "description": "执行命令。如果在指定时间内未完成，将返回当前输出并转入后台运行。",
      "parameters": {
        "type": "object",
        "properties": {
          "command": { "type": "string", "description": "要执行的命令" },
          "timeout": { 
            "type": "integer", 
            "description": "最大等待时长（秒），默认 10。超过此时间则返回 task_id 并转后台。",
            "default": 10
          }
        },
        "required": ["command"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "check_task",
      "description": "管理或查看已启动任务的状态。可以获取实时标准输出/错误日志，或者强制终止正在运行的任务。",
      "parameters": {
        "type": "object",
        "properties": {
          "task_id": { 
            "type": "string", 
            "description": "exec 函数返回的任务唯一标识符。" 
          },
          "order": { 
            "type": "string", 
            "enum": ["check", "kill"],
            "description": "操作类型：'check' 用于获取当前输出日志；'kill' 用于立即停止该任务。" 
          }
        },
        "required": ["task_id", "order"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "delete",
      "description": "删除文件",
      "parameters": {
        "type": "object",
        "properties": {
          "file": { "type": "string", "description": "要删除的文件名" }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search",
      "description": "通过duckduckgo搜索信息",
      "parameters": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "description": "第一行是需要返回的条数，第二行是搜索关键词" }
        },
        "required": ["text"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "cex_info",
      "description": "获取加密货币价格，以及用户的交易所余额，获取余额用balance表示，多个查询用空格隔开",
      "parameters": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "description": "币种名" }
        },
        "required": ["text"]
      }
    }
  }
]